{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist", "dist-deploy"],
  "compilerOptions": {
    // worker/index.ts needs the Cloudflare Fetcher / ExecutionContext globals.
    "types": ["@cloudflare/workers-types", "@webgpu/types"]
  }
}
